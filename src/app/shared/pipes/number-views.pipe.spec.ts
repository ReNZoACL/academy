import { NumberViewsPipe } from './number-views.pipe';

describe('NumberViewsPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberViewsPipe();
    expect(pipe).toBeTruthy();
  });
});
